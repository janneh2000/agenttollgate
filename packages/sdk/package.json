{
  "name": "agenttollgate",
  "version": "0.1.0",
  "description": "Agent SDK for AgentTollgate — handles 402 → CheckoutWithLocus → retry transparently.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "license": "MIT",
  "files": ["dist", "README.md"]
}
